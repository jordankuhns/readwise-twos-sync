[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && python combined_server.py"
restartPolicyType = "never"

[variables]
PORT = "8000"